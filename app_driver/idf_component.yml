dependencies:
  idf:
    version: ">=5.0"
  # http_client.c includes cJSON.h; see main/idf_component.yml for why this
  # is declared explicitly (ESP-IDF v6.0 removed the built-in "json" component).
  espressif/cjson: "^1.7.19"
